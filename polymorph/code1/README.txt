Two shellcode files in here. I started with the original code, shortened it by 15 bytes (this is code1-smaller.nasm) and then polymorphed it (into the final-polymorph).

There are additional comments in code1-smaller.nasm to make it easier to follow some of the changes in the final polymorph
